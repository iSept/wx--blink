<!--components/navi/index.wxml-->
<view class="container">
  <image class="icon" bind:tap="onLeft" src="{{latest?disLeftSrc:leftSrc}}" />
  <Text class="title">{{title}}</Text>
  <image class="icon" bind:tap="onRight" src="{{first?disRightSrc:rightSrc}}" />
</view>
